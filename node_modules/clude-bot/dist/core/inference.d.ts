export type InferenceProvider = 'anthropic' | 'venice' | 'auto';
export interface InferenceConfig {
    /** Primary provider (default: 'auto' — tries Venice first, falls back to Anthropic) */
    primary?: InferenceProvider;
    /** Fallback provider if primary fails */
    fallback?: InferenceProvider;
    /** Venice model override */
    veniceModel?: string;
    /** Anthropic model override */
    anthropicModel?: string;
}
export interface GenerateOptions {
    /** User message/prompt */
    userMessage: string;
    /** Additional context to include */
    context?: string;
    /** Feature-specific instructions */
    featureInstruction?: string;
    /** System prompt override */
    systemPrompt?: string;
    /** Max tokens */
    maxTokens?: number;
    /** Force a specific provider */
    provider?: InferenceProvider;
}
export declare function configureInference(config: InferenceConfig): void;
export declare function getInferenceConfig(): InferenceConfig;
/**
 * Generate a response using the configured inference provider(s).
 * Handles automatic fallback if primary fails.
 */
export declare function generate(opts: GenerateOptions): Promise<string>;
/**
 * Quick helper for simple prompts.
 */
export declare function ask(prompt: string, opts?: Partial<GenerateOptions>): Promise<string>;
//# sourceMappingURL=inference.d.ts.map