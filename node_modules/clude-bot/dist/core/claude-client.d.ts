import Anthropic from '@anthropic-ai/sdk';
/** @internal Bot injects its personality (base-prompt.ts) here at startup. */
export declare function _setSystemPromptProvider(fn: () => string): void;
/** @internal Bot injects its closer logic here at startup. */
export declare function _setResponsePostProcessor(fn: (text: string) => string): void;
/** @internal SDK escape hatch — allows Cortex to inject a pre-configured Anthropic client. */
export declare function _setAnthropicClient(client: Anthropic, model?: string): void;
export interface GenerateOptions {
    userMessage: string;
    context?: string;
    moodModifier?: string;
    tierModifier?: string;
    agentModifier?: string;
    featureInstruction?: string;
    memoryContext?: string;
    maxTokens?: number;
}
export declare function generateResponse(options: GenerateOptions): Promise<string>;
/**
 * Single-purpose LLM call to rate memory importance (Park et al. 2023).
 * Returns a raw string (expected: single integer 1-10).
 * Uses low maxTokens and temperature 0 for deterministic, fast scoring.
 */
export declare function generateImportanceScore(description: string): Promise<string>;
export declare function generateThread(options: GenerateOptions): Promise<string[]>;
//# sourceMappingURL=claude-client.d.ts.map